[project]
name = "ssh-teste-deploy-py"
version = "0.1.0"
description = "CRUD simples (FastAPI + SQLite) para testar deploy via GitHub Actions + SSH"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]
