name: Deploy to Windows (self-hosted)

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: [self-hosted, windows]
    steps:
      - uses: actions/checkout@v4

      - name: Sync deps and restart service
        shell: powershell
        run: ./deploy/restart-service.ps1
